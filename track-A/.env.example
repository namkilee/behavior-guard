############################################################
# BehaviorGuard - Research Mode (Track A)
# 이 파일은 pipeline.sh 에서 단 한 번만 로드됨
# 하위 script(build/export/python)는 env를 상속받음
############################################################


############################
# 실험 대상 날짜 (선택)
############################
# pipeline.sh 실행 시 인자로 전달하지 않을 경우 사용
# 예: DAY=2026-01-06
DAY=


############################
# 🗄 ClickHouse 연결 정보
# (현재는 parquet 모드라도 export 시 필요)
############################

# ClickHouse 서버 주소
CH_HOST=127.0.0.1

# Native TCP 포트 (보통 9000)
# HTTP는 8123
CH_PORT=9000

# 사용자 계정
CH_USER=default

# 비밀번호 (없으면 빈 값 가능)
CH_PASSWORD=

# 사용할 데이터베이스 이름
CH_DATABASE=default


############################
# SQL 경로 설정
############################

# 세션 기본 쿼리 원본 파일
# build_sql.sh에서 export 템플릿에 삽입됨
SESSIONS_QUERY_FILE=sql/sessions_query.sql

# raw export 템플릿
RAW_TEMPLATE_FILE=sql/export_sessions_raw.sql

# feature export 템플릿
FEAT_TEMPLATE_FILE=sql/export_session_features.sql

# build된 SQL 출력 디렉토리
SQL_BUILD_DIR=sql/build


############################
# Export 결과 저장 경로
############################

# parquet 및 결과 파일 저장 디렉토리
OUT_DIR=out


############################
#  Track A 모델 설정
############################

# Top-K 이상 세션 개수
TOPK=100

# CSV 저장 여부 (1=저장, 0=저장 안함)
SAVE_CSV=1

# Parquet 저장 여부 (1=저장, 0=저장 안함)
SAVE_PARQUET=1


############################
# Parquet 파일 경로 (선택)
# 기본값: out/session_features_{DAY}.parquet
#         out/sessions_raw_{DAY}.parquet
# 필요 시 override 가능
############################

FEATURES_PARQUET=
RAW_PARQUET=


############################
# ClickHouse 직접 실행 모드용 (선택)
# --source clickhouse 일 때 사용
############################

# feature SQL 파일 경로
FEATURE_SQL_PATH=sql/build/export_session_features.built.sql

# drilldown SQL 파일 경로
DRILLDOWN_SQL_PATH=sql/bg_sessions_drilldown.sql
